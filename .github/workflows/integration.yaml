name: Rust Continuous Integration

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  run-tests-and-coverage:
    uses: fermi-ad/.github/.github/workflows/rust-integration.yaml@b99946ba8722aeb25ac95245fa01862054748f92
    secrets: inherit
    permissions:
      contents: write
      pull-requests: write

# If using a git submodule, add 
#    with:
#      pull-git-submodules: true

# If using an internal git repo as a Rust dependency, add
#    with:
#      references-internal-git-repo: true
